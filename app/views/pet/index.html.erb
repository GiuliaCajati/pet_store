<h1>Alex and Giulia's Pet Store</h1>

<div>Today's Date is <%= Date.today.to_s(:long) %>.</div>

<ul>
    <% @pets.each do |pet| %>
        <li>
            <%= link_to pet.name, pet_path(pet) %> 
        </li>
    <% end %>
</ul>